1. register

2. login

3. searching (basics)
"offers" & "searches":
- results with start only?
- results with destination only?
- results with start & destination?
- results with when changing date?
- is place selection working?

4. advertising (basics)
- advertising with mandatory information possible?
- advertising with information in all fields possible?
- is stored data (mytrips) equal to entered information?
- mytrips: editing & changing trips possible?

5. miscellaneous
- is contact form sending emails?



